// Model Object
// Representation of data structure and any validation on properties.
module.exports = {

  // Example Properties:

  // name: 'string',
  // email: 'email',
  // password: 'string',
  // tags: 'array',
  // handle: 'alphanum'

  // The above would utilize the internal validation. The validation
  // schemas such as 'string' and 'array' use native JS detection, while
  // more custom validations can be set via the regEx object in /config.js

};